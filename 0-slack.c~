#include "monty.h"
/**
 * _slack - slack mode
 * @stack: pointer to pointer to stack
 * @line_number: line number
 */
void _slack(stack_t **stack, unsigned int line_number)
{
	data->mode = 0;
	(void)slack;
	(void)line_number;
}
